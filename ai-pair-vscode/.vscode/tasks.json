{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build ai-pair-vscode",
      "type": "npm",
      "script": "build",
      "group": "build",
      "problemMatcher": [
        "$tsc"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "detail": "tsc && copyfiles -u 1 src/webview/**/* dist"
    }
  ]
}